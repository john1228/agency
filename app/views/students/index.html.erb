<ul id="myTab" class="nav nav-tabs">
  <li class="active">
    <a href="#order" data-toggle="tab">买课学员</a>
  </li>
  <li><a href="#appointment" data-toggle="tab">关注学员</a></li>
</ul>
<div id="myTabContent" class="tab-content">
  <div class="tab-pane fade in active" id="order">
    <%= render partial: 'order' %>
  </div>
  <div class="tab-pane fade" id="appointment">
    <%= render partial: 'appointment' %>
  </div>
</div>




<div class="row">
  <div class="col-lg-12 top10 tdzty">
    <h3 class="poloklh4">学员信息</h3>

    <div class="panel panel-default top15">
      <table class="table table-hover txt-zt">
        <thead>
        <tr class="prito">
          <th class="text-center">头像</th>
          <th class="text-center">昵称</th>
          <th class="text-center">性别</th>
          <th class="text-center">注册日期</th>
          <th>签名</th>
          <th class="text-center">操作</th>
        </tr>
        </thead>
        <tbody class="pritbl">
        <%
           @follows.each { |follow|
             user = follow.user
        %>
            <tr>
              <td class="text-center tyeq"><%= image_tag(user.profile.avatar.thumb.url, class: 'img-circle  img-w') %></td>
              <td class="text-center tyeq"><%= user.profile.name %></td>
              <td class="text-center tyeq"><%= user.profile.gender.eql?(1) ? '女' : '男' %></td>
              <td class="text-center tyeq"><%= user.profile.birthday.strftime('%Y-%m-%d') rescue '' %></td>
              <td><%= truncate(user.profile.signature) %></td>
            </tr>
        <% } %>
        </tbody>
      </table>
    </div>
    <div class=" padding5">
      <%= will_paginate @follows %>
    </div>
  </div>
</div>