<div class="row">
  <div class="col-lg-12 top10 tdzty">
    <h3 class="poloklh4">私教信息</h3>

    <div class="panel panel-default top15">
      <table class="table table-hover txt-zt">
        <thead>
        <tr class="prito">
          <th class="text-center">头像</th>
          <th class="text-center">昵称</th>
          <th class="text-center">性别</th>
          <th class="text-center">注册日期</th>
          <th>签名</th>
          <th class="text-center">操作</th>
        </tr>
        </thead>
        <tbody class="pritbl">
        <% @coaches.each { |coach| %>
            <tr>
              <td class="text-center tyeq"><%= image_tag(coach.profile.avatar.thumb.url, class: 'img-circle  img-w') %></td>
              <td class="text-center tyeq"><%= coach.profile.name %></td>
              <td class="text-center tyeq"><%= coach.profile.gender.eql?(1) ? '女' : '男' %></td>
              <td class="text-center tyeq"><%= coach.profile.birthday.strftime('%Y-%m-%d') rescue '' %></td>
              <td><%= truncate(coach.profile.signature) %></td>
              <td class="text-center">
                <a confirm="您确定要删除吗?" rel="nofollow" data-method="delete" href="/coaches/<%= coach.id %>"><i class="iconfont pritbt ">&#xe60d;</i></a>
              </td>
            </tr>
        <% } %>
        </tbody>
      </table>
    </div>
    <div class=" padding5">
      <%= will_paginate @coaches, renderer: BootstrapPagination::Rails %>
    </div>
  </div>
</div>