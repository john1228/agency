"zh-CN":
  activerecord:
    attributes:
      admin_user:
        status:
          approving: "审核中"
          approved: "审核通过"
          rejected: "审核不通过"
      service:
        state:
          approving: "审核中"
          approved: "审核通过"
          rejected: "审核不通过"
